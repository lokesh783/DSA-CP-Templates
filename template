// Lokesh Bhai aaj phodega
// Jai Maa Bhavani
// Jai ekling ji
// Baujiraj ki daya se
// search - ctrl f
// select - ctrl sft l
#include <bits/stdc++.h>
#include <iostream>
#define ll long long
using namespace std;
#define f(i, a, b) for (ll i = a; i < b; i++)

// vectors
#define vll vector<ll>
#define vvll vector<vector<ll>>
#define pb push_back

#define yup cout << "YES";
#define shit cout << "NO";
#define el endl;
#define do2 cout <<
#define lo cin >>
ll mod = 1e9 + 7;

ll GCD(ll a, ll b){if (a == 0)return b;return GCD(b % a, a);}
ll LCM(ll a, ll b){ll val = GCD(a, b);return a * b / (val * val);}

ll sum_of_dig(ll n) { ll ans = 0, rem; while (n) { rem = n % 10; n /= 10; ans += rem; } return ans; }

// O(sqrt(N))
bool is_prime(ll nimboda) { if (nimboda == 1) return false; if (nimboda == 2 || nimboda == 3) return true; f(i, 2, sqrt(nimboda) + 1) { if (nimboda % i == 0) return false; }return true;}

// O(NLOGN)
void SieveOfEratosthenes(vector<bool> mp){ll n = 1e9;mp[0] = mp[1] = false;for (ll p = 2; p * p <= n; p++){if (mp[p] == true){for (ll i = p * p; i <= n; i += p)mp[i] = false;}}}

// O(1)
bool ky_2_ki_pow_hai(ll n){if (n == 0)return false;return (ceil(log2(n)) == floor(log2(n)));}

bool cmp(pair<ll, ll> &a, pair<ll, ll> &b) { return a.second > b.second; }
void inA(ll arr[], ll n) { f(i, 0, n) cin >> arr[i]; }
void inV(vector<ll> &arr, ll n) { f(i, 0, n) cin >> arr[i]; }


void apna_ans_aayega()
{
    
}

// MAin GFUnnction
int main()
{
    ll kitni_baar;
    cin >> kitni_baar;
    while (kitni_baar--)
    {
        apna_ans_aayega();
    }
    // apna_ans_aayega();
    return 0;
}
